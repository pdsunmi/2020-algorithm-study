-- 코드를 입력하세요
SELECT OUTS.ANIMAL_ID, OUTS.NAME
FROM ANIMAL_INS AS INS
RIGHT JOIN ANIMAL_OUTS AS OUTS
ON INS.ANIMAL_ID = OUTS.ANIMAL_ID
WHERE INS.DATETIME>OUTS.DATETIME 
ORDER BY INS.DATETIME;

-- 코드를 입력하세요
SELECT OUTS.ANIMAL_ID, OUTS.NAME
FROM ANIMAL_INS AS INS
RIGHT JOIN ANIMAL_OUTS AS OUTS
ON INS.ANIMAL_ID = OUTS.ANIMAL_ID
WHERE INS.ANIMAL_ID IS NULL;

-- 코드를 입력하세요
SELECT ANIMAL_TYPE
      ,CASE WHEN NAME IS NULL THEN 'No name' else NAME END NAME 
      ,SEX_UPON_INTAKE
FROM ANIMAL_INS

-- 코드를 입력하세요
SELECT BRANCH_ID, SUM(SALARY) AS TOTAL FROM EMPLOYEES
GROUP BY BRANCH_ID
ORDER BY BRANCH_ID ASC
;

-- 코드를 입력하세요
SELECT *, COUNT(*) AS COUNT
FROM (SELECT HOUR(DATETIME) AS HOUR FROM ANIMAL_OUTS) NEW
GROUP BY HOUR HAVING HOUR>=9
ORDER BY HOUR
;

-- 코드를 입력하세요
SET @NUM = -1;
SELECT @NUM:=@NUM+1 AS HOUR,  
(
    SELECT COUNT(*) FROM ANIMAL_OUTS
    WHERE HOUR(DATETIME)=@NUM
) AS COUNT
FROM ANIMAL_OUTS
WHERE @NUM<24
ORDER BY HOUR
;